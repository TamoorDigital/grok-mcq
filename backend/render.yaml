services:
  - type: web
    name: grok-mcq-backend
    env: python

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn app:app --timeout 180 --workers 1 --worker-class sync --keep-alive 5

    envVars:
      - key: GROQ_API_KEY
        sync: false
