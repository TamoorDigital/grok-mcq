services:
  - type: web
    name: grok-mcq-backend
    env: python

    buildCommand: |
      apt-get update
      apt-get install -y tesseract-ocr tesseract-ocr-eng
      pip install -r requirements.txt

    startCommand: gunicorn app:app --timeout 120 --workers 2 --worker-class sync

    envVars:
      - key: HF_API_KEY
        sync: false