services:
  - type: web
    name: grok-mcq-backend
    env: python

    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app

    aptPackages:
      - tesseract-ocr
      - tesseract-ocr-eng

    envVars:
      - key: GEMINI_API_KEY
        sync: false